# _legacy/

This folder contains files that have been superseded by the active codebase.
They are preserved here for reference — **do not delete them**.

| File / Folder | Superseded by | Reason |
|---|---|---|
| `ava_v2/` | `AGENT_B/` | Stale snapshot of AGENT_B; not imported anywhere |
| `ava_coverage_patch.py` | `AGENT_F/ava_coverage_patch.py` | Older, incomplete version missing `format_ava_schema()` and `atomic_write()` |

If you need to recover any of this content, simply copy it back to the project root.
